/**
 * Retired in the Studio Light redesign — film grain is no longer part of the
 * design language. Kept as a no-op so legacy imports compile until swept.
 */
const GrainOverlay = (_props: { className?: string }) => null;

export default GrainOverlay;
